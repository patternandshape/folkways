import { releasePopularity } from 'folkways/helpers/release-popularity';
import { module, test } from 'qunit';

module('Unit | Helper | release popularity');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = releasePopularity([42]);
  assert.ok(result);
});
