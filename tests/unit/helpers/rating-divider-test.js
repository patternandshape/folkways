import { ratingDivider } from 'folkways/helpers/rating-divider';
import { module, test } from 'qunit';

module('Unit | Helper | rating divider');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = ratingDivider([42]);
  assert.ok(result);
});
